// Module ID: 10363
// Function ID: 79953
// Name: _isNativeReflectConstruct
// Dependencies: [6, 7, 15, 17, 18, 5, 27, 683, 4344, 477, 10265, 491, 10364, 5078, 2]

// Module 10363 (_isNativeReflectConstruct)
import getActivityIndicator from "get ActivityIndicator";
import _isNativeReflectConstruct from "_isNativeReflectConstruct";
import _possibleConstructorReturn from "_possibleConstructorReturn";
import _getPrototypeOf from "_getPrototypeOf";
import _inherits from "_inherits";
import asyncGeneratorStep from "asyncGeneratorStep";
import get_ActivityIndicator from "get ActivityIndicator";
import closure_10 from "_isNativeReflectConstruct";
import { NativePermissionStatus } from "NativePermissionStatus";
import tmp4 from "AutomaticLifecycleManager";

const require = arg1;
function _isNativeReflectConstruct() {
  let closure_0 = !valueOf.call(Reflect.construct(Boolean, [], () => {

  }));
  function _isNativeReflectConstruct() {
    return closure_0;
  }
  const result = _isNativeReflectConstruct();
}
function showNotification(uri) {
  function _getAndroidScreenshot() {
    // CreateGeneratorClosureLongIndex (0x67)
    const obj = outer1_8(tmp);
    return obj(...arguments);
  }
  uri = undefined;
  if (null != uri) {
    uri = uri.uri;
  }
  // CreateGeneratorClosureLongIndex (0x67)
  const timerId = setTimeout(callback3(tmp), 1200);
}
const NativeModules = get_ActivityIndicator.NativeModules;
const nativeEventEmitter = new get_ActivityIndicator.NativeEventEmitter(NativeModules.ScreenshotHelper);
tmp4 = new tmp4();
let result = require("_possibleConstructorReturn").fileFinishedImporting("modules/bug_reporter/native/BugReportManager.tsx");

export default tmp4;
