// Module ID: 12669
// Function ID: 12670
// Dependencies: [4288, 12, 12097, 2]

// Module 12669
import updateInvite from "updateInvite";
import importDefaultResult from "apply";

let closure_3 = require("apply").throttle((code) => {
  invite = invite.getInvite(code.code);
  importDefault(12097).open({ invite });
}, 1000, { trailing: false });
const obj = {
  onOpenHubInvite(invite) {
    callback(invite);
  }
};
const result = require("module_12097").fileFinishedImporting("modules/hub/HubUtils.native.tsx");

export default obj;
