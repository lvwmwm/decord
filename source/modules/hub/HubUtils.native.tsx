// Module ID: 12735
// Function ID: 12736
// Dependencies: [4327, 12, 12161, 2]

// Module 12735
import updateInvite from "updateInvite";
import importDefaultResult from "apply";

let closure_3 = require("apply").throttle((code) => {
  invite = invite.getInvite(code.code);
  importDefault(12161).open({ invite });
}, 1000, { trailing: false });
const obj = {
  onOpenHubInvite(invite) {
    callback(invite);
  }
};
const result = require("module_12161").fileFinishedImporting("modules/hub/HubUtils.native.tsx");

export default obj;
