// Module ID: 12599
// Function ID: 12600
// Dependencies: [4287, 12, 12031, 2]

// Module 12599
import updateInvite from "updateInvite";
import importDefaultResult from "apply";

let closure_3 = require("apply").throttle((code) => {
  invite = invite.getInvite(code.code);
  importDefault(12031).open({ invite });
}, 1000, { trailing: false });
const obj = {
  onOpenHubInvite(invite) {
    callback(invite);
  }
};
const result = require("module_12031").fileFinishedImporting("modules/hub/HubUtils.native.tsx");

export default obj;
