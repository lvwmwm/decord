// Module ID: 10309
// Function ID: 79599
// Name: getDisplayName
// Dependencies: []
// Exports: default

// Module 10309 (getDisplayName)
function getDisplayName(tag) {
  if (null == tag.tag) {
    let str3 = tag.label;
  } else {
    const _HermesInternal = HermesInternal;
    str3 = "" + tag.label + " " + tag.tag;
  }
  let str4 = str3;
  if (str3.includes("_START")) {
    str4 = `Start ${str3.replace("_START", "")}`;
  }
  let text = str4;
  if (str4.includes("_END")) {
    text = `Finish ${str4.replace("_END", "")}`;
  }
  return text;
}
const _module = require(dependencyMap[3]);
const result = _module.fileFinishedImporting("modules/debug/serializeAppStartLogs.tsx");

export default function serializeAppStartLogs(arg0) {
  let flag = arg1;
  let flag2 = arg2;
  const require = arg0;
  if (arg1 === undefined) {
    flag = true;
  }
  const importDefault = flag;
  if (flag2 === undefined) {
    flag2 = true;
  }
  const dependencyMap = flag2;
  const logGroups = importDefault(dependencyMap[0]).logGroups;
  const mapped = logGroups.map((arg0) => {
    let index;
    let logs;
    let nativeLogs;
    let serverTrace;
    let timestamp;
    ({ index, timestamp, logs, nativeLogs, serverTrace } = arg0);
    let tmp = timestamp;
    if (0 === index) {
      timestamp = undefined;
      const found = flag(flag2[1]).find(logs, (log) => {
        log = log.log;
        return log.indexOf("Logger loaded") >= 0;
      });
      if (null != found) {
        timestamp = found.timestamp;
      }
      if (null == timestamp) {
        timestamp = arg0;
      }
      tmp = timestamp;
      const arr = flag(flag2[1]);
    }
    const sum = index + 1;
    const tmp7 = function serializeLogGroup(arg0, arg1) {
      const items = [];
      let num = 0;
      if (0 < arg0.length) {
        while (true) {
          let tmp = arg0[num];
          let tmp2 = arg0[num + 1];
          if (null != tmp2) {
            let str = tmp.log;
            if (tmp2.log === str.replace("Start ", "Finish ")) {
              let str2 = tmp2.log;
              tmp2.log = str2.replace("Finish ", "");
              let arr = items.push(tmp2);
              let sum = num + 1;
              num = sum + 1;
              if (num >= arg0.length) {
                break;
              }
            }
          }
          arr = items.push(tmp);
          sum = num;
        }
      }
      const mapped = items.map((timestamp) => {
        const obj = {};
        let str = "";
        if (null != timestamp.timestamp) {
          const result = (timestamp.timestamp - arg1) / 1000;
          str = result.toFixed(3);
        }
        obj.totalTime = str;
        let str2 = "";
        if (null != timestamp.delta) {
          const _String = String;
          const _Math = Math;
          str2 = String(Math.round(timestamp.delta));
        }
        obj.deltaTime = str2;
        let str3 = "";
        if (timestamp.emoji.length > 0) {
          const _HermesInternal = HermesInternal;
          str3 = "" + timestamp.emoji + " ";
        }
        obj.log = "" + str3 + timestamp.prefix + timestamp.log + "\n";
        return obj;
      });
      const maxResult = num2(num3[1]).max(mapped.map((totalTime) => totalTime.totalTime.length));
      let num2 = 0;
      if (null != maxResult) {
        num2 = maxResult;
      }
      const obj = num2(num3[1]);
      const maxResult1 = num2(num3[1]).max(mapped.map((deltaTime) => deltaTime.deltaTime.length));
      let num3 = 0;
      if (null != maxResult1) {
        num3 = maxResult1;
      }
      const mapped1 = mapped.map((arg0) => {
        let deltaTime;
        let log;
        let totalTime;
        ({ totalTime, deltaTime, log } = arg0);
        const obj = num2(num3[1]);
        const padStartResult = num2(num3[1]).padStart(totalTime, num2);
        return "" + padStartResult + " " + num2(num3[1]).padStart(deltaTime, num3) + " " + log;
      });
      return mapped1.join("");
    }(function joinLogs(logs, nativeLogs, flag, flag2) {
      const substr = logs.slice();
      const set = new Set(nativeLogs.map(set));
      let closure_4 = "";
      let closure_5 = [];
      const item = nativeLogs.forEach((autoGenerated) => {
        const str = set(autoGenerated);
        let tmp = arg2;
        if (!arg2) {
          tmp = !autoGenerated.autoGenerated;
        }
        let startsWithResult = str.startsWith("Start ");
        if (startsWithResult) {
          startsWithResult = !str.includes("RUN_JS_BUNDLE");
        }
        if (startsWithResult) {
          startsWithResult = set.has(str.replace("Start ", "Finish "));
        }
        let num = 0;
        let tmp4 = tmp;
        if (str.startsWith("Finish ")) {
          num = 0;
          tmp4 = tmp;
          if (!str.includes("RUN_JS_BUNDLE")) {
            num = 0;
            tmp4 = tmp;
            if (set.has(str.replace("Finish ", "Start "))) {
              let _var = _var.substring(2);
              let arr = arr.pop();
              num = 0;
              tmp4 = tmp;
              if (null != arr) {
                const diff = autoGenerated.timestamp - arr.timestamp;
                let tmp11 = tmp;
                if (!tmp) {
                  tmp11 = diff > 5 && !function isSuperNoisyLog(arg0) {
                    const items = [null, null];
                    return items.some(() => { ... });
                  }(str);
                  const tmp12 = diff > 5 && !function isSuperNoisyLog(arg0) {
                    const items = [null, null];
                    return items.some(() => { ... });
                  }(str);
                }
                arr.shouldKeep = arr.shouldKeep || tmp11;
                num = diff;
                tmp4 = tmp11;
              }
            }
          }
        }
        const obj = { emoji: "\u2615", timestamp: autoGenerated.timestamp };
        let tmp13;
        if (num > 0) {
          tmp13 = num;
        }
        obj.delta = tmp13;
        obj.prefix = _var;
        obj.log = str;
        obj.shouldKeep = tmp4;
        arr = substr;
        let num4 = 0;
        let num5 = 0;
        if (0 < substr.length) {
          while (true) {
            let timestamp = arr[num4].timestamp;
            if (null == timestamp) {
              num4 = num4 + 1;
              num5 = num4;
              if (num4 >= arr.length) {
                break;
              }
            } else {
              num5 = num4;
              if (timestamp > obj.timestamp) {
                break;
              }
            }
            break;
          }
        }
        arr.splice(num5, 0, obj);
        if (startsWithResult) {
          _var = `${closure_4}| `;
          arr = arr.push(obj);
        }
      });
      let closure_6 = false;
      return substr.filter((log) => {
        let tmp = !closure_6;
        if (!tmp) {
          log = log.log;
          tmp = !log.includes("\u21AA");
        }
        if (tmp) {
          let tmp3 = !arg3;
          if (tmp3) {
            tmp3 = "\u{1F3A8}" === log.emoji;
          }
          if (!tmp3) {
            tmp3 = false === log.shouldKeep;
          }
          closure_6 = tmp3;
          tmp = !tmp3;
        }
        return tmp;
      });
    }(logs, nativeLogs, flag, flag2), tmp);
    const combined = "Trace #" + sum + " started " + arg0(flag2[2]).getTimestampString(timestamp) + "\n" + tmp7;
    let sum1 = combined;
    if (null != serverTrace) {
      const _HermesInternal = HermesInternal;
      sum1 = combined + "\n Server trace for trace #" + index + 1 + serverTrace;
    }
    return sum1;
  });
  return mapped.join("\n\n");
};
