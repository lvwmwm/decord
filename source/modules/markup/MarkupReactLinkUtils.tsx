// Module ID: 11116
// Function ID: 11117
// Name: isLinkTrusted
// Dependencies: [8572, 7828, 2]
// Exports: isLinkTrusted

// Module 11116 (isLinkTrusted)
import set from "set" /* 2 */;
import isLinkTrusted from "isLinkTrusted" /* 8572 */;

const result = set.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    isLinkTrusted;
    if (null == target.title) {
      let title = tmp2(7828).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(7828);
    }
    title = target.title;
    tmp2 = require;
  }
};
