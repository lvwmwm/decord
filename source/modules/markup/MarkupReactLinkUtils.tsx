// Module ID: 11081
// Function ID: 11082
// Name: isLinkTrusted
// Dependencies: [8938, 8096, 2]
// Exports: isLinkTrusted

// Module 11081 (isLinkTrusted)
const result = require("set").fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    require(8938) /* isLinkTrusted */;
    if (null == target.title) {
      let title = tmp2(8096).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(8096);
    }
    title = target.title;
    tmp2 = require;
  }
};
