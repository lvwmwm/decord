// Module ID: 10994
// Function ID: 10995
// Name: isLinkTrusted
// Dependencies: [8855, 8017, 2]
// Exports: isLinkTrusted

// Module 10994 (isLinkTrusted)
const result = require("set").fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    require(8855) /* isLinkTrusted */;
    if (null == target.title) {
      let title = tmp2(8017).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(8017);
    }
    title = target.title;
    tmp2 = require;
  }
};
