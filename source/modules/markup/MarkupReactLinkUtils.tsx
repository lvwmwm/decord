// Module ID: 10800
// Function ID: 10801
// Name: isLinkTrusted
// Dependencies: [8775, 6902, 2]
// Exports: isLinkTrusted

// Module 10800 (isLinkTrusted)
const result = require("set").fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    require(8775) /* isLinkTrusted */;
    if (null == target.title) {
      let title = tmp2(6902).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(6902);
    }
    title = target.title;
    tmp2 = require;
  }
};
