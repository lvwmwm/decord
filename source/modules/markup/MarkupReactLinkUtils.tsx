// Module ID: 11134
// Function ID: 11135
// Name: isLinkTrusted
// Dependencies: [8587, 7842, 2]
// Exports: isLinkTrusted

// Module 11134 (isLinkTrusted)
import set from "set" /* 2 */;
import isLinkTrusted from "isLinkTrusted" /* 8587 */;

const result = set.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    isLinkTrusted;
    if (null == target.title) {
      let title = tmp2(7842).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(7842);
    }
    title = target.title;
    tmp2 = require;
  }
};
