// Module ID: 9509
// Function ID: 9510
// Name: isLinkTrusted
// Dependencies: [8200, 7768, 2]
// Exports: isLinkTrusted

// Module 9509 (isLinkTrusted)
const result = require("set").fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    require(8200) /* isLinkTrusted */;
    if (null == target.title) {
      let title = tmp2(7768).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(7768);
    }
    title = target.title;
    tmp2 = require;
  }
};
