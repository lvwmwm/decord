// Module ID: 11134
// Function ID: 11135
// Name: isLinkTrusted
// Dependencies: [8977, 8135, 2]
// Exports: isLinkTrusted

// Module 11134 (isLinkTrusted)
const result = require("set").fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    require(8977) /* isLinkTrusted */;
    if (null == target.title) {
      let title = tmp2(8135).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(8135);
    }
    title = target.title;
    tmp2 = require;
  }
};
