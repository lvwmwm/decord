// Module ID: 4207
// Function ID: 36569
// Name: toInteger
// Dependencies: []

// Module 4207 (toInteger)

export default function toInteger(arg0) {
  let num = 0;
  const tmp = require(dependencyMap[0])(arg0);
  const result = tmp % 1;
  if (tmp == tmp) {
    let diff = tmp;
    if (result) {
      diff = tmp - result;
    }
    num = diff;
  }
  return num;
};
