// Module ID: 7680
// Function ID: 7681
// Name: baseSortBy
// Dependencies: []

// Module 7680 (baseSortBy)

export default function baseSortBy(arr) {
  let tmp4;
  const sorted = arr.sort(arg1);
  let diff = tmp2 - 1;
  if (+arr.length) {
    do {
      arr[diff] = arr[diff].value;
      tmp4 = +diff;
      diff = tmp4 - 1;
    } while (tmp4);
  }
  return arr;
};
