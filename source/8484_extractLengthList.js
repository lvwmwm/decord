// Module ID: 8484
// Function ID: 67567
// Name: extractLengthList
// Dependencies: []

// Module 8484 (extractLengthList)
const re0 = /\s+/;
const re1 = /,/g;
arg5.default = function extractLengthList(str) {
  if (Array.isArray(str)) {
    return str;
  } else if ("number" === typeof str) {
    const items = [str];
    let parts = items;
  } else if ("string" === tmp) {
    const str3 = str.trim();
    parts = str.trim().replace(closure_1, " ").split(closure_0);
    const str5 = str.trim().replace(closure_1, " ");
  } else {
    parts = [];
  }
};
