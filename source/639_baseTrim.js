// Module ID: 639
// Function ID: 640
// Name: baseTrim
// Dependencies: [640]

// Module 639 (baseTrim)
const re2 = /^\s+/;

export default function baseTrim(arr) {
  let replaced = arr;
  if (arr) {
    replaced = arr.slice(0, require(640) /* trimmedEndIndex */(arr) + 1).replace(closure_2, "");
    const str = arr.slice(0, require(640) /* trimmedEndIndex */(arr) + 1);
  }
  return replaced;
};
