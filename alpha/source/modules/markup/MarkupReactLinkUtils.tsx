// Module ID: 11095
// Function ID: 11096
// Name: MarkupReactLinkUtils
// Dependencies: [7810, 7424, 2]
// Exports: isLinkTrusted

// Module 11095 (MarkupReactLinkUtils)
import MaskedLinkUtils from "MaskedLinkUtils" /* 7810 */;
import size from "module_2" /* 2 */;

const result = size.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    MaskedLinkUtils;
    if (null == target.title) {
      let title = tmp2(7424).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(7424);
    }
    title = target.title;
    tmp2 = require;
  }
};
