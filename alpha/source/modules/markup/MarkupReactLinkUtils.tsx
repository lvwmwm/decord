// Module ID: 11952
// Function ID: 11953
// Name: MarkupReactLinkUtils
// Dependencies: [8720, 8335, 2]
// Exports: isLinkTrusted

// Module 11952 (MarkupReactLinkUtils)
import MaskedLinkUtils from "MaskedLinkUtils" /* 8720 */;
import size from "module_2" /* 2 */;

const result = size.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    MaskedLinkUtils;
    if (null == target.title) {
      let title = tmp2(8335).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(8335);
    }
    title = target.title;
    tmp2 = require;
  }
};
